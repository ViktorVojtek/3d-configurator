import styled, { StyledComponent } from 'styled-components';

export const TitleWrapper: StyledComponent<'div', any, {}, never> = styled.div`
  position: fixed;
  top: 1rem;
  left: 1rem;
`;
export const H2 = styled.h2`
  font-family: Arial, Helvetica, sans-serif;
  font-weight: 200;
`;